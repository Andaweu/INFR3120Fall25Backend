// server/routes/api/users.api.js
const express = require('express');
const router = express.Router();

// Placeholder route — expand when needed
router.get('/', (req, res) => {
  res.json({ message: 'Users API is working.' });
});

module.exports = router;